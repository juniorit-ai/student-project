
                        // Primitive types
                        let numberType = 42; // Number
                        let stringType = 'Hello, World!'; // String
                        let booleanType = true; // Boolean

                        console.log('Number:', numberType);
                        console.log('String:', stringType);
                        console.log('Boolean:', booleanType);

                        // Complex types
                        let arrayType = [1, 2, 3, 4, 5]; // Array
                        let objectType = { name: 'Alice', age: 17 }; // Object

                        console.log('Array:', arrayType);
                        console.log('Object:', objectType);

                        // Accessing array elements
                        console.log('First element of array:', arrayType[0]);

                        // Accessing object properties
                        console.log('Name from object:', objectType.name);
                    