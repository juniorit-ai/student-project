// Define a car object using an object literal
var car = { make: 'Toyota', model: 'Corolla', year: 2021 };

// Adding a new property to car
 car.color = 'blue';

// Updating the year property
 car.year = 2022;

// Deleting the model property
 delete car.model;

// Logging the modified car object
 console.log(car);