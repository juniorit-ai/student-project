// Example of using ByteBoss for code optimization
// Imagine a scenario where ByteBoss suggests a more efficient algorithm for a task
function sum(arr) {
  return arr.reduce((acc, val) => acc + val, 0);
}
let numbers = [1, 2, 3, 4, 5];
console.log('Sum of numbers:', sum(numbers));