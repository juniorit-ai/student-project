// Example of using ByteBoss for code suggestions
// Imagine a scenario where ByteBoss suggests a more efficient way to sort an array
let numbers = [4, 2, 5, 1, 3];
numbers.sort((a, b) => a - b);
console.log('Sorted numbers:', numbers);