// Array operations that need debugging
function addElement(arr, element) {
  // Code to add element to array
}

function removeElement(arr, index) {
  // Code to remove element from array
}

module.exports = { addElement, removeElement };
